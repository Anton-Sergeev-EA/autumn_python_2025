#todo: Модифицировать программу таким образом, чтобы она выводила
# приветствие "Hello", которое только что записали в файл text.txt.

# Первый вариант: с явным использованием f.close().
f = open("text.txt", "w+t")
f.write("Hello\n")
f.seek(0)
content = f.read()
f.close()
print(content.strip())

f.close()

# Второй вариант: с использованием with.
# with open("text.txt", "w+t") as f:
#     f.write("Hello\n")
#     f.seek(0)  # Возвращаемся в начало файла
#     content = f.read()
# print(content.strip())

# Второй вариант предпочтительнее, так как:
# 1) Более безопасен (файл гарантированно закроется даже при ошибке).
# 2) Код выглядит чище и понятнее.
# 3) Меньше вероятность забыть закрыть файл.
